$(document).ready(function(){
	
	// setInterval(function(){
	// 	$('.cartCountProducts').text('1');
	// }, 2000);
  $('#slider .owl-carousel').owlCarousel({
    loop:true,
    margin:0,
    dots:true,
    nav:false,
    autoplay:true,
    autoplayTimeout:7000,
    items:1,
  })

  $('#dopzakaz .owl-carousel').owlCarousel({
      loop:false,
      margin:20,
      nav:true,
    dots:false,
      responsive:{
      0:{
      items:2
      },
      700:{
      items:3
      },
      1000:{
      items:4
      }
      }
      })

$(window).scroll(function(){
    //var client_h = document.body.clientHeight;
    var ram = window.pageYOffset || document.documentElement.scrollTop;
    if ( ram > 75 ) {
      $('#fix-head').css({"position":"fixed","top":"0"});
      $('#fix-head').addClass("fixed");
    };
    if ( ram < 75 ) {
	  if (window.outerWidth < 900) {
		$('#fix-head').css({"position":"absolute","top":"0"});
		$('#fix-head').removeClass("fixed");
	  }
	  else {
		$('#fix-head').css({"position":"absolute","top":"75px"});
		$('#fix-head').removeClass("fixed");
	  }
		
    };

    // var pizzaLink = $('#pizza').offset().top - 60;
    // var komboLink = $('#kombo').offset().top - 60;
    // var snacksLink = $('#snacks').offset().top - 60;
    // var saucesLink = $('#sauces').offset().top - 60;
    // var desertsLink = $('#deserts').offset().top - 60;
    // var drinksLink = $('#drinks').offset().top - 60;
    // var deliveryLink = $('#delivery').offset().top - 60;
    // var aboutusLink = $('#aboutus').offset().top - 60;
    // var footerLink = $('#footer').offset().top - 60;

    // if ( ram < footerLink ) {
    //   $('a.go_to').removeClass("active");
    //   $('a#aboutus-link').addClass("active");
    // };
    // if ( ram < aboutusLink ) {
    //   $('a.go_to').removeClass("active");
    //   $('a#delivery-link').addClass("active");
    // };
    // if ( ram < deliveryLink ) {
    //   $('a.go_to').removeClass("active");
    //   $('a#drinks-link').addClass("active");
    // };
    // if ( ram < drinksLink ) {
    //   $('a.go_to').removeClass("active");
    //   $('a#deserts-link').addClass("active");
    // };
    // if ( ram < desertsLink ) {
    //   $('a.go_to').removeClass("active");
    //   $('a#sauces-link').addClass("active");
    // };
    // if ( ram < saucesLink ) {
    //   $('a.go_to').removeClass("active");
    //   $('a#snacks-link').addClass("active");
    // };
    // if ( ram < snacksLink ) {
    //   $('a.go_to').removeClass("active");
    //   $('a#kombo-link').addClass("active");
    // };
    // if ( ram < komboLink ) {
    //   $('a.go_to').removeClass("active");
    //   $('a#pizza-link').addClass("active");
    // };
    // if ( ram < pizzaLink ) {
    //   $('a.go_to').removeClass("active");
    // };

  });

	//РѕС‚РєСЂС‹С‚СЊ РјРµРЅСЋ --РЅР°С‡Р°Р»Рѕ--
	  $('#menubutton').click(function(){
		$('div#menu').css({"left":"0"})
		$('div#shadow').css({"z-index":"8", "opacity":"1"})
	  });
	//РѕС‚РєСЂС‹С‚СЊ РјРµРЅСЋ --РєРѕРЅРµС†--

	//Р·Р°РєСЂС‹С‚СЊ РјРµРЅСЋ --РЅР°С‡Р°Р»Рѕ--
	  $('#menuclose').click(function(){
		var client_w=document.body.clientWidth;
		$('div#menu').css({"left":"-250px"})
		$('div#shadow').css({"z-index":"-1", "opacity":"0"})
	  });

	  $('div#shadow').click(function(){
		var client_w=document.body.clientWidth;
		$('div#menu').css({"left":"-250px"})
		$('div#shadow').css({"z-index":"-1", "opacity":"0"})
	  });
	//Р·Р°РєСЂС‹С‚СЊ РјРµРЅСЋ --РєРѕРЅРµС†--

  $('.go_to').click( function(){
    var scroll_el = $(this).attr('href');
    if ($(scroll_el).length != 0) {
      $('html, body').animate({ scrollTop: $(scroll_el).offset().top - 60 }, 500);
    }
    return false;
  });

  

    $('span.pz.add').click(function(){
	//РґРѕРґР±Р°РІР»РµРЅРёРµ РЅРѕРІРѕРіРѕ Р±Р»РѕРєР° РїРёС†С†С‹ РІ РєРѕСЂР·РёРЅСѓ

		//РїРѕР»СѓС‡Р°РµРј id С‚РѕРІР°СЂР°
		var tovar_id = $(this).attr('id');
		var idfn = tovar_id.substring(0, 5);
		//СЃРѕР±РёСЂР°РµРј РґР°РЅРЅС‹Рµ С‚РѕРІР°СЂР°
		var tovar_name = $('#' + idfn + '-nm').text();
		var tovar_diam = $('#' + tovar_id + 'lbl').text();
		var tovar_price = $('#' + tovar_id + 'prc').text();
		var tovar_art = $('#' + tovar_id + 'prc').attr('data-art');

		//РјРµРЅСЏРµРј РєРЅРѕРїРєСѓ РЅР° СЃС‡РµС‚С‹
		$('span#' + tovar_id).css({"display":"none","pointer-events":"none"});
		$('#' + tovar_id + 'cnt').css({"display":"table","pointer-events":"fill"});
		console.log('test');
		//СЃРѕР·РґР°РµРј Р±Р»РѕРє РІ РєРѕСЂР·РёРЅРµ
		if($('#cartdesc .blocks').find('#' + tovar_art).length > 0) {
			console.log('test');
			return false;
		}
		$('#cartdesc .blocks').append('<div id="crt-' + tovar_id + '" class="cart-block" data-cartart="' + tovar_art + '"><span id="' + tovar_id + 'del" class="delete">x</span><h4 id="crt-' + tovar_id + 'nm" class="name">' + tovar_name + '</h4><p id="crt-' + tovar_id + 'diam" class="diam">' + tovar_diam + '</p><div class="quant"><span id="' + tovar_id + 'mns-crt" class="minus">-</span><p id="crt-' + tovar_id + 'klv" class="kolvo">1</p><span id="' + tovar_id + 'pls-crt" class="plus">+</span></div><p id="crt-' + tovar_id + 'prc" class="cart-price" data-price="' + tovar_price + '">' + tovar_price + '</p></div>');


		//РїСЂРѕРІРµСЂСЏРµРј РЅР°Р»РёС‡РёРµ РѕР±С‰РµР№ СЃСѓРјРјС‹
		if(!$('#cartdesc').children('.total-price').length > 0) {
		$('#cartdesc').append('<div class="total-price"><p class="total">РЎСѓРјРјР° Р·Р°РєР°Р·Р°:</p><p class="price"></p></div>');
		}

		//РѕР±С‰Р°СЏ СЃСѓРјРјР°
		var sum = 0;
		$('.cart-price').each(function(){
		 sum += parseInt($(this).text());
		});
		$('.total-price p.price').text(sum + ' С‚Рі');
		$('span.total').text(sum + ' С‚Рі');
		  
	  //СЃР±РѕСЂ РґР°РЅРЅС‹С… РєРѕСЂР·РёРЅС‹ РІ РјР°СЃСЃРёРІ
	  var kuk_ar = [];
	  var kuk_key = 0;
	  $('.cart-block').each(function(){
		 var cb_id = $(this).attr('id')
		 var pizza = cb_id.substring(0, 7);
		 kuk_ar[kuk_key] = cb_id;
		 kuk_key++;
		 if (pizza=='crt-pzb') {
			 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + 'nm').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + 'prc').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + 'klv').text();
			 kuk_key++;
		 } else {
			 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + '-nm').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + '-prc').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + '-klv').text();
			 kuk_key++;
		 }
	  });
	 changecooksval("wowcart", kuk_ar);

		//РѕР±С‰РµРµ РєРѕР»РёС‡РµСЃС‚РІРѕ
		var kolvo = 0;
		$('.kolvo').each(function(){
		  kolvo += parseInt($(this).text());
		});
		$('.cartCountProducts').text(kolvo);
		if (kolvo == 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂ');
		if (kolvo > 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂР°');
		if (kolvo > 4) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
		
		$('span.delete').click(function(){
		  var block_id = $(this).attr('id').substring(0, 7);
		  $('#crt-' + block_id).remove();
		  $('span#' + block_id).css({"display":"table","pointer-events":"fill"});
		  $('#' + block_id + 'cnt').css({"display":"none","pointer-events":"none"});
		  $('#' + block_id + 'klv').text('1');

		  if(!$('div').is('.cart-block')) {
			$('.total-price').remove();
		  }

		  //РѕР±С‰Р°СЏ СЃСѓРјРјР°
		  var sum = 0;
		  $('.cart-price').each(function(){
			 sum += parseInt($(this).text());
		  });
		  $('.total-price p.price').text(sum + ' С‚Рі');
		  $('span.total').text(sum + ' С‚Рі');
		  
		  //СЃР±РѕСЂ РґР°РЅРЅС‹С… РєРѕСЂР·РёРЅС‹ РІ РјР°СЃСЃРёРІ
		  var kuk_ar = [];
		  var kuk_key = 0;
		  $('.cart-block').each(function(){
			 var cb_id = $(this).attr('id')
			 var pizza = cb_id.substring(0, 7);
			 kuk_ar[kuk_key] = cb_id;
			 kuk_key++;
			 if (pizza=='crt-pzb') {
				 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'nm').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'prc').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'klv').text();
				 kuk_key++;
			 } else {
				 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-nm').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-prc').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-klv').text();
				 kuk_key++;
			 }
		  });
		 changecooksval("wowcart", kuk_ar);

		  //РѕР±С‰РµРµ РєРѕР»РёС‡РµСЃС‚РІРѕ
		  var kolvo = 0;
		  $('.kolvo').each(function(){
			  kolvo += parseInt($(this).text());
		  });
		  if (kolvo == 0) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
		  if (kolvo == 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂ');
		  if (kolvo > 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂР°');
		  if (kolvo > 4) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
		});

		$('span#' + tovar_id + 'pls-crt').click(function(){
		  var count_id = $(this).attr('id').substring(0, 7);
		  var b_count = 0;
		  var summ = 0;
		  var tov_prc = 0;
		  b_count = parseInt($('#crt-' + count_id + 'klv').text());
		  summ = b_count + 1;
		  $('#crt-' + count_id + 'klv').text(summ);
		  $('#' + count_id + 'klv').text(summ);
		  tov_prc = parseInt($('#crt-' + count_id + 'prc').attr('data-price'));
		  $('#crt-' + count_id + 'prc').text(tov_prc * summ + ' С‚Рі');

		  //РѕР±С‰Р°СЏ СЃСѓРјРјР°
		  var sum = 0;
		  $('.cart-price').each(function(){
			 sum += parseInt($(this).text());
		  });
		  $('.total-price p.price').text(sum + ' С‚Рі');
		  $('span.total').text(sum + ' С‚Рі');
		  
		  //СЃР±РѕСЂ РґР°РЅРЅС‹С… РєРѕСЂР·РёРЅС‹ РІ РјР°СЃСЃРёРІ
		  var kuk_ar = [];
		  var kuk_key = 0;
		  $('.cart-block').each(function(){
			 var cb_id = $(this).attr('id')
			 var pizza = cb_id.substring(0, 7);
			 kuk_ar[kuk_key] = cb_id;
			 kuk_key++;
			 if (pizza=='crt-pzb') {
				 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'nm').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'prc').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'klv').text();
				 kuk_key++;
			 } else {
				 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-nm').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-prc').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-klv').text();
				 kuk_key++;
			 }
		  });
		 changecooksval("wowcart", kuk_ar);

		  //РѕР±С‰РµРµ РєРѕР»РёС‡РµСЃС‚РІРѕ
		  var kolvo = 0;
		  $('.kolvo').each(function(){
			  kolvo += parseInt($(this).text());
		  });
		  if (kolvo == 0) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
		  if (kolvo == 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂ');
		  if (kolvo > 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂР°');
		  if (kolvo > 4) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
		  
		});
		$('span#' + tovar_id + 'mns-crt').click(function(){
		  var count_id = $(this).attr('id').substring(0, 7);
		  var b_count = 0;
		  var summ = 0;
		  var tov_prc = 0;
		  b_count = parseInt($('#crt-' + count_id + 'klv').text());
		  if (b_count!=1) {
			summ = +b_count - 1;
			$('#crt-' + count_id + 'klv').text(summ);
			$('#' + count_id + 'klv').text(summ);
			tov_prc = parseInt($('#crt-' + count_id + 'prc').attr('data-price'));
			$('#crt-' + count_id + 'prc').text(tov_prc * summ + ' С‚Рі');
		  } else {
			$('#crt-' + count_id).remove();
			$('span#' + count_id).css({"display":"table","pointer-events":"fill"});
			$('#' + count_id + 'cnt').css({"display":"none","pointer-events":"none"});
			$('#' + count_id + 'klv').text('1');

			if(!$('div').is('.cart-block')) {
			  $('.total-price').remove();
			}
		  }

		  //РѕР±С‰Р°СЏ СЃСѓРјРјР°
		  var sum = 0;
		  $('.cart-price').each(function(){
			 sum += parseInt($(this).text());
		  });
		  $('.total-price p.price').text(sum + ' С‚Рі');
		  $('span.total').text(sum + ' С‚Рі');
		  
		  //СЃР±РѕСЂ РґР°РЅРЅС‹С… РєРѕСЂР·РёРЅС‹ РІ РјР°СЃСЃРёРІ
		  var kuk_ar = [];
		  var kuk_key = 0;
		  $('.cart-block').each(function(){
			 var cb_id = $(this).attr('id')
			 var pizza = cb_id.substring(0, 7);
			 kuk_ar[kuk_key] = cb_id;
			 kuk_key++;
			 if (pizza=='crt-pzb') {
				 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'nm').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'prc').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'klv').text();
				 kuk_key++;
			 } else {
				 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-nm').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-prc').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-klv').text();
				 kuk_key++;
			 }
		  });
		 changecooksval("wowcart", kuk_ar);

		  //РѕР±С‰РµРµ РєРѕР»РёС‡РµСЃС‚РІРѕ
		  var kolvo = 0;
		  $('.kolvo').each(function(){
			  kolvo += parseInt($(this).text());
		  });
		  if (kolvo == 0) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
		  if (kolvo == 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂ');
		  if (kolvo > 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂР°');
		  if (kolvo > 4) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
		});

    });//РєРѕРЅРµС† РґРѕР±Р°РІР»РµРЅРёСЏ РїРёС†С†С‹ РІ РєРѕСЂРёР·РЅСѓ


    $('span.sm.add').click(function(e){
	  //РґРѕРґР±Р°РІР»РµРЅРёРµ РЅРѕРІРѕРіРѕ Р±Р»РѕРєР° РІ РєРѕСЂР·РёРЅСѓ
	  
      //РїРѕР»СѓС‡Р°РµРј id С‚РѕРІР°СЂР°
      var tovar_id = $(this).attr('id');
      //СЃРѕР±РёСЂР°РµРј РґР°РЅРЅС‹Рµ С‚РѕРІР°СЂР°
      var tovar_name = $('#' + tovar_id + '-nm').text();
      var tovar_price = $('#' + tovar_id + '-prc').text();
	  var tovar_art = $('#' + tovar_id + '-prc').attr('data-art');

      //РјРµРЅСЏРµРј РєРЅРѕРїРєСѓ РЅР° СЃС‡РµС‚С‹
      $('span#' + tovar_id).css({"display":"none","pointer-events":"none"});
      $('#' + tovar_id + '-cnt').css({"display":"table","pointer-events":"fill"});

      //СЃРѕР·РґР°РµРј Р±Р»РѕРє РІ РєРѕСЂР·РёРЅРµ
      $('#cartdesc .blocks').append('<div id="crt-' + tovar_id + '" class="cart-block" data-cartart="' + tovar_art + '"><span id="' + tovar_id + 'del" class="delete">x</span><h4 id="crt-' + tovar_id + '-nm" class="name">' + tovar_name + '</h4><div class="quant"><span id="' + tovar_id + 'mns-crt" class="minus">-</span><p id="crt-' + tovar_id + '-klv" class="kolvo">1</p><span id="' + tovar_id + 'pls-crt" class="plus">+</span></div><p id="crt-' + tovar_id + '-prc" class="cart-price" data-price="' + tovar_price + '">' + tovar_price + '</p></div>');


      //РїСЂРѕРІРµСЂСЏРµРј РЅР°Р»РёС‡РёРµ РѕР±С‰РµР№ СЃСѓРјРјС‹
      if(!$('#cartdesc').children('.total-price').length > 0) {
       $('#cartdesc').append('<div class="total-price"><p class="total">РЎСѓРјРјР° Р·Р°РєР°Р·Р°:</p><p class="price"></p></div>');
      }

      //РѕР±С‰Р°СЏ СЃСѓРјРјР°
      var sum = 0;
      $('.cart-price').each(function(){
         sum += parseInt($(this).text());
      });
      $('.total-price p.price').text(sum + ' С‚Рі');
      $('span.total').text(sum + ' С‚Рі');
		  
	  //СЃР±РѕСЂ РґР°РЅРЅС‹С… РєРѕСЂР·РёРЅС‹ РІ РјР°СЃСЃРёРІ
	  var kuk_ar = [];
	  var kuk_key = 0;
	  $('.cart-block').each(function(){
		 var cb_id = $(this).attr('id')
		 var pizza = cb_id.substring(0, 7);
		 kuk_ar[kuk_key] = cb_id;
		 kuk_key++;
		 if (pizza=='crt-pzb') {
			 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + 'nm').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + 'prc').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + 'klv').text();
			 kuk_key++;
		 } else {
			 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + '-nm').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + '-prc').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + '-klv').text();
			 kuk_key++;
		 }
	  });
	 changecooksval("wowcart", kuk_ar);
	  
      //РѕР±С‰РµРµ РєРѕР»РёС‡РµСЃС‚РІРѕ
      var kolvo = 0;
      $('.kolvo').each(function(){
          kolvo += parseInt($(this).text());
      });
      if (kolvo == 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂ');
      if (kolvo > 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂР°');
      if (kolvo > 4) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
	  $('.cartCountProducts').text(kolvo);
        $('span.delete').click(function(){
          var block_id = $(this).attr('id').substring(0, 5);
          $('#crt-' + block_id).remove();
          $('span#' + block_id).css({"display":"table","pointer-events":"fill"});
          $('#' + block_id + '-cnt').css({"display":"none","pointer-events":"none"});
          $('#' + block_id + '-klv').text('1');

          if(!$('div').is('.cart-block')) {
            $('.total-price').remove();
          }

          //РѕР±С‰Р°СЏ СЃСѓРјРјР°
          var sum = 0;
          $('.cart-price').each(function(){
             sum += parseInt($(this).text());
          });
          $('.total-price p.price').text(sum + ' С‚Рі');
          $('span.total').text(sum + ' С‚Рі');
		  
		  //СЃР±РѕСЂ РґР°РЅРЅС‹С… РєРѕСЂР·РёРЅС‹ РІ РјР°СЃСЃРёРІ
		  var kuk_ar = [];
		  var kuk_key = 0;
		  $('.cart-block').each(function(){
			 var cb_id = $(this).attr('id')
			 var pizza = cb_id.substring(0, 7);
			 kuk_ar[kuk_key] = cb_id;
			 kuk_key++;
			 if (pizza=='crt-pzb') {
				 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'nm').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'prc').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'klv').text();
				 kuk_key++;
			 } else {
				 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-nm').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-prc').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-klv').text();
				 kuk_key++;
			 }
		  });
		 changecooksval("wowcart", kuk_ar);

          //РѕР±С‰РµРµ РєРѕР»РёС‡РµСЃС‚РІРѕ
          var kolvo = 0;
          $('.kolvo').each(function(){
              kolvo += parseInt($(this).text());
          });
          if (kolvo == 0) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
          if (kolvo == 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂ');
          if (kolvo > 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂР°');
          if (kolvo > 4) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
        });

        $('span#' + tovar_id + 'pls-crt').click(function(){
          var count_id = $(this).attr('id').substring(0, 5);
          var b_count = 0;
          var summ = 0;
          var tov_prc = 0;
          b_count = parseInt($('#crt-' + count_id + '-klv').text());
          summ = b_count + 1;
          $('#crt-' + count_id + '-klv').text(summ);
          $('#' + count_id + '-klv').text(summ);
          tov_prc = parseInt($('#crt-' + count_id + '-prc').attr('data-price'));
          $('#crt-' + count_id + '-prc').text(tov_prc * summ + ' С‚Рі');

          //РѕР±С‰Р°СЏ СЃСѓРјРјР°
          var sum = 0;
          $('.cart-price').each(function(){
             sum += parseInt($(this).text());
          });
          $('.total-price p.price').text(sum + ' С‚Рі');
          $('span.total').text(sum + ' С‚Рі');
		  
		  //СЃР±РѕСЂ РґР°РЅРЅС‹С… РєРѕСЂР·РёРЅС‹ РІ РјР°СЃСЃРёРІ
		  var kuk_ar = [];
		  var kuk_key = 0;
		  $('.cart-block').each(function(){
			 var cb_id = $(this).attr('id')
			 var pizza = cb_id.substring(0, 7);
			 kuk_ar[kuk_key] = cb_id;
			 kuk_key++;
			 if (pizza=='crt-pzb') {
				 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'nm').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'prc').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'klv').text();
				 kuk_key++;
			 } else {
				 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-nm').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-prc').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-klv').text();
				 kuk_key++;
			 }
		  });
		 changecooksval("wowcart", kuk_ar);

          //РѕР±С‰РµРµ РєРѕР»РёС‡РµСЃС‚РІРѕ
          var kolvo = 0;
          $('.kolvo').each(function(){
              kolvo += parseInt($(this).text());
          });
          if (kolvo == 0) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
          if (kolvo == 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂ');
          if (kolvo > 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂР°');
		  if (kolvo > 4) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
		  $('.cartCountProducts').text(kolvo);

        });
        $('span#' + tovar_id + 'mns-crt').click(function(){
          var count_id = $(this).attr('id').substring(0, 5);
          var b_count = 0;
          var summ = 0;
          var tov_prc = 0;
          b_count = parseInt($('#crt-' + count_id + '-klv').text());
          if (b_count!=1) {
            summ = +b_count - 1;
            $('#crt-' + count_id + '-klv').text(summ);
            $('#' + count_id + '-klv').text(summ);
            tov_prc = parseInt($('#crt-' + count_id + '-prc').attr('data-price'));
            $('#crt-' + count_id + '-prc').text(tov_prc * summ + ' С‚Рі');
          } else {
            $('#crt-' + count_id).remove();
            $('span#' + count_id).css({"display":"table","pointer-events":"fill"});
            $('#' + count_id + '-cnt').css({"display":"none","pointer-events":"none"});
            $('#' + count_id + '-klv').text('1');

            if(!$('div').is('.cart-block')) {
              $('.total-price').remove();
            }
          }

          //РѕР±С‰Р°СЏ СЃСѓРјРјР°
          var sum = 0;
          $('.cart-price').each(function(){
             sum += parseInt($(this).text());
          });
          $('.total-price p.price').text(sum + ' С‚Рі');
          $('span.total').text(sum + ' С‚Рі');
		  
		  //СЃР±РѕСЂ РґР°РЅРЅС‹С… РєРѕСЂР·РёРЅС‹ РІ РјР°СЃСЃРёРІ
		  var kuk_ar = [];
		  var kuk_key = 0;
		  $('.cart-block').each(function(){
			 var cb_id = $(this).attr('id')
			 var pizza = cb_id.substring(0, 7);
			 kuk_ar[kuk_key] = cb_id;
			 kuk_key++;
			 if (pizza=='crt-pzb') {
				 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'nm').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'prc').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'klv').text();
				 kuk_key++;
			 } else {
				 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-nm').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-prc').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-klv').text();
				 kuk_key++;
			 }
		  });
		 changecooksval("wowcart", kuk_ar);

          //РѕР±С‰РµРµ РєРѕР»РёС‡РµСЃС‚РІРѕ
          var kolvo = 0;
          $('.kolvo').each(function(){
              kolvo += parseInt($(this).text());
          });
          if (kolvo == 0) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
          if (kolvo == 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂ');
          if (kolvo > 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂР°');
		  if (kolvo > 4) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
		  $('.cartCountProducts').text(kolvo);
        });

    });//РєРѕРЅРµС† РґРѕР±Р°РІР»РµРЅРёСЏ РІ РєРѕСЂРёР·РЅСѓ

	
	
	 $('span.ppz').click(function(e){
	  //РґРѕРґР±Р°РІР»РµРЅРёРµ РЅРѕРІРѕРіРѕ Р±Р»РѕРєР° РІ РёС‚РѕРіРѕРІСѓСЋ РєРѕСЂР·РёРЅСѓ
	 
	
		
      //РїРѕР»СѓС‡Р°РµРј id С‚РѕРІР°СЂР°
      var tovar_id = $(this).attr('id');
      //СЃРѕР±РёСЂР°РµРј РґР°РЅРЅС‹Рµ С‚РѕРІР°СЂР°
      var tovar_name = $('#' + tovar_id + '-nm').text();
      var tovar_price = $('#' + tovar_id + '-prc').text();
	  var data_price = tovar_price.substr(0, tovar_price.length - 3);

      //РјРµРЅСЏРµРј РєРЅРѕРїРєСѓ РЅР° СЃС‡РµС‚С‹
      $('span#' + tovar_id).css({"display":"none","pointer-events":"none"});
      $('#' + tovar_id + '-cnt').css({"display":"table","pointer-events":"fill"});

	  //СЃРѕР·РґР°РµРј Р±Р»РѕРє РІ РєРѕСЂР·РёРЅРµ
		
	 
	  if($('div#cart-table').find('#rw-' + tovar_id).length > 0) {
			return false;
	  }
      $('div#cart-table').append('<div id="rw-' + tovar_id + '" class="rw-block"><h4 id="rw-' + tovar_id + '-nm" class="rw-name">' + tovar_name + '</h4><span id="' + tovar_id + 'del" class="rw-delete">x</span><p id="rw-' + tovar_id + '-klv" class="rw-kolvo">1</p><p id="rw-' + tovar_id + '-prc" class="rw-price" data-price="' + data_price + '">' + tovar_price + '</p></div>');	  
	  
	 
		
        $('span#' + tovar_id + '-pls').click(function(){
          var count_id = $(this).attr('id').substring(0, 5);
          var b_count = 0;
          var summ = 0;
          var tov_prc = 0;
          b_count = parseInt($('#rw-' + count_id + '-klv').text());
          summ = b_count + 1;
          $('#rw-' + count_id + '-klv').text(summ);
          $('#' + count_id + '-klv').text(summ);
          tov_prc = parseInt($('#rw-' + count_id + '-prc').attr('data-price'));
          $('#rw-' + count_id + '-prc').text(tov_prc * summ + ' С‚Рі');

		  
        });
		
		
        $('span#' + tovar_id + '-mns').click(function(){
          var count_id = $(this).attr('id').substring(0, 5);
          var b_count = 0;
          var summ = 0;
          var tov_prc = 0;
          b_count = parseInt($('#rw-' + count_id + '-klv').text());
          if (b_count!=1) {
            summ = +b_count - 1;
            $('#rw-' + count_id + '-klv').text(summ);
            $('#' + count_id + '-klv').text(summ);
            tov_prc = parseInt($('#rw-' + count_id + '-prc').attr('data-price'));
            $('#rw-' + count_id + '-prc').text(tov_prc * summ + ' С‚Рі');
          } else {
            $('#rw-' + count_id).remove();
            $('span#' + count_id).css({"display":"table","pointer-events":"fill"});
            $('#' + count_id + '-cnt').css({"display":"none","pointer-events":"none"});
            $('#' + count_id + '-klv').text('1');
          }

        });
	
	$('span.rw-delete').click(function(){
	  var block_id = $(this).attr('id').substring(0, 5);
	  $('#crt-' + block_id).remove();
	  $('#rw-' + block_id).remove();
	  $('span#' + block_id).css({"display":"table","pointer-events":"fill"});
	  $('#' + block_id + '-cnt').css({"display":"none","pointer-events":"none"});
	  $('#' + block_id + '-klv').text('1');

	  if(!$('div').is('.cart-block')) {
		$('.total-price').remove();
	  }

	  //РѕР±С‰Р°СЏ СЃСѓРјРјР°
	  var sum = 0;
	  $('.cart-price').each(function(){
		 sum += parseInt($(this).text());
	  });
	  $('.total-price p.price').text(sum + ' С‚Рі');
	  $('span.total').text(sum + ' С‚Рі');
	  $('span#cartsumm').text(sum + ' С‚Рі');
	  
	  //СЃР±РѕСЂ РґР°РЅРЅС‹С… РєРѕСЂР·РёРЅС‹ РІ РјР°СЃСЃРёРІ
	  var kuk_ar = [];
	  var kuk_key = 0;
	  $('.cart-block').each(function(){
		 var cb_id = $(this).attr('id')
		 var pizza = cb_id.substring(0, 7);
		 kuk_ar[kuk_key] = cb_id;
		 kuk_key++;
		 if (pizza=='crt-pzb') {
			 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + 'nm').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + 'prc').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + 'klv').text();
			 kuk_key++;
		 } else {
			 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + '-nm').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + '-prc').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + '-klv').text();
			 kuk_key++;
		 }
	  });
	 changecooksval("wowcart", kuk_ar);

	  //РѕР±С‰РµРµ РєРѕР»РёС‡РµСЃС‚РІРѕ
	  var kolvo = 0;
	  $('.kolvo').each(function(){
		  kolvo += parseInt($(this).text());
	  });
	  if (kolvo == 0) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
	  if (kolvo == 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂ');
	  if (kolvo > 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂР°');
	  if (kolvo > 4) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
	  $('.cartCountProducts').text(kolvo);
	});

	
    });//РєРѕРЅРµС† РґРѕР±Р°РІР»РµРЅРёСЏ РІ РєРѕСЂРёР·РЅСѓ
	
	
	
		
    $('span.bplus').click(function(){
      var count_id = $(this).attr('id').substring(0, 7);
      var b_count = 0;
      var summ = 0;
      var tov_prc = 0;
      b_count = parseInt($('#crt-' + count_id + 'klv').text());
      summ = b_count + 1;
      $('#crt-' + count_id + 'klv').text(summ);
      $('#' + count_id + 'klv').text(summ);
      tov_prc = parseInt($('#crt-' + count_id + 'prc').attr('data-price'));
      $('#crt-' + count_id + 'prc').text(tov_prc * summ + ' С‚Рі');

      //РѕР±С‰Р°СЏ СЃСѓРјРјР°
      var sum = 0;
      $('.cart-price').each(function(){
         sum += parseInt($(this).text());
      });
      $('.total-price p.price').text(sum + ' С‚Рі');
      $('span.total').text(sum + ' С‚Рі');
		  
	  //СЃР±РѕСЂ РґР°РЅРЅС‹С… РєРѕСЂР·РёРЅС‹ РІ РјР°СЃСЃРёРІ
	  var kuk_ar = [];
	  var kuk_key = 0;
	  $('.cart-block').each(function(){
		 var cb_id = $(this).attr('id')
		 var pizza = cb_id.substring(0, 7);
		 kuk_ar[kuk_key] = cb_id;
		 kuk_key++;
		 if (pizza=='crt-pzb') {
			 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + 'nm').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + 'prc').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + 'klv').text();
			 kuk_key++;
		 } else {
			 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + '-nm').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + '-prc').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + '-klv').text();
			 kuk_key++;
		 }
	  });
	 changecooksval("wowcart", kuk_ar);

      //РѕР±С‰РµРµ РєРѕР»РёС‡РµСЃС‚РІРѕ
      var kolvo = 0;
      $('.kolvo').each(function(){
          kolvo += parseInt($(this).text());
      });
      if (kolvo == 0) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
      if (kolvo == 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂ');
      if (kolvo > 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂР°');
	  if (kolvo > 4) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
	  $('.cartCountProducts').text(kolvo);

    });
    $('span.bminus').click(function(){
      var count_id = $(this).attr('id').substring(0, 7);
      var b_count = 0;
      var summ = 0;
      var tov_prc = 0;
      b_count = parseInt($('#crt-' + count_id + 'klv').text());
      if (b_count!=1) {
        summ = +b_count - 1;
        $('#crt-' + count_id + 'klv').text(summ);
        $('#' + count_id + 'klv').text(summ);
        tov_prc = parseInt($('#crt-' + count_id + 'prc').attr('data-price'));
        $('#crt-' + count_id + 'prc').text(tov_prc * summ + ' С‚Рі');
      } else {
        $('#crt-' + count_id).remove();
        $('span#' + count_id).css({"display":"table","pointer-events":"fill"});
        $('#' + count_id + 'cnt').css({"display":"none","pointer-events":"none"});
        $('#' + count_id + 'klv').text('1');

        if(!$('div').is('.cart-block')) {
          $('.total-price').remove();
        }
      }

      //РѕР±С‰Р°СЏ СЃСѓРјРјР°
      var sum = 0;
      $('.cart-price').each(function(){
         sum += parseInt($(this).text());
      });
      $('.total-price p.price').text(sum + ' С‚Рі');
      $('span.total').text(sum + ' С‚Рі');
		  
	  //СЃР±РѕСЂ РґР°РЅРЅС‹С… РєРѕСЂР·РёРЅС‹ РІ РјР°СЃСЃРёРІ
	  var kuk_ar = [];
	  var kuk_key = 0;
	  $('.cart-block').each(function(){
		 var cb_id = $(this).attr('id')
		 var pizza = cb_id.substring(0, 7);
		 kuk_ar[kuk_key] = cb_id;
		 kuk_key++;
		 if (pizza=='crt-pzb') {
			 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + 'nm').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + 'prc').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + 'klv').text();
			 kuk_key++;
		 } else {
			 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + '-nm').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + '-prc').text();
			 kuk_key++;
			 kuk_ar[kuk_key] = $('#' + cb_id + '-klv').text();
			 kuk_key++;
		 }
	  });
	 changecooksval("wowcart", kuk_ar);

      //РѕР±С‰РµРµ РєРѕР»РёС‡РµСЃС‚РІРѕ
      var kolvo = 0;
      $('.kolvo').each(function(){
          kolvo += parseInt($(this).text());
      });
      if (kolvo == 0) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
      if (kolvo == 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂ');
      if (kolvo > 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂР°');
	  if (kolvo > 4) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
	  $('.cartCountProducts').text(kolvo);
    });

        $('span.cplus').click(function(){
          var count_id = $(this).attr('id').substring(0, 5);
          var b_count = 0;
          var summ = 0;
          var tov_prc = 0;
          b_count = parseInt($('#crt-' + count_id + '-klv').text());
          summ = b_count + 1;
          $('#crt-' + count_id + '-klv').text(summ);
          $('#' + count_id + '-klv').text(summ);
          tov_prc = parseInt($('#crt-' + count_id + '-prc').attr('data-price'));
          $('#crt-' + count_id + '-prc').text(tov_prc * summ + ' С‚Рі');	  

          //РѕР±С‰Р°СЏ СЃСѓРјРјР°
          var sum = 0;
          $('.cart-price').each(function(){
             sum += parseInt($(this).text());
          });
          $('.total-price p.price').text(sum + ' С‚Рі');
          $('span.total').text(sum + ' С‚Рі');
		  
		  //СЃР±РѕСЂ РґР°РЅРЅС‹С… РєРѕСЂР·РёРЅС‹ РІ РјР°СЃСЃРёРІ
		  var kuk_ar = [];
		  var kuk_key = 0;
		  $('.cart-block').each(function(){
			 var cb_id = $(this).attr('id')
			 var pizza = cb_id.substring(0, 7);
			 kuk_ar[kuk_key] = cb_id;
			 kuk_key++;
			 if (pizza=='crt-pzb') {
				 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'nm').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'prc').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'klv').text();
				 kuk_key++;
			 } else {
				 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-nm').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-prc').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-klv').text();
				 kuk_key++;
			 }
		  });
		 changecooksval("wowcart", kuk_ar);

          //РѕР±С‰РµРµ РєРѕР»РёС‡РµСЃС‚РІРѕ
          var kolvo = 0;
          $('.kolvo').each(function(){
              kolvo += parseInt($(this).text());
          });
          if (kolvo == 0) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
          if (kolvo == 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂ');
          if (kolvo > 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂР°');
		  if (kolvo > 4) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
		  $('.cartCountProducts').text(kolvo);

        });
        $('span.cminus').click(function(){
          var count_id = $(this).attr('id').substring(0, 5);
          var b_count = 0;
          var summ = 0;
          var tov_prc = 0;
          b_count = parseInt($('#crt-' + count_id + '-klv').text());
          if (b_count!=1) {
            summ = +b_count - 1;
            $('#crt-' + count_id + '-klv').text(summ);
            $('#' + count_id + '-klv').text(summ);
            tov_prc = parseInt($('#crt-' + count_id + '-prc').attr('data-price'));
            $('#crt-' + count_id + '-prc').text(tov_prc * summ + ' С‚Рі');
          } else {
            $('#crt-' + count_id).remove();
            $('span#' + count_id).css({"display":"table","pointer-events":"fill"});
            $('#' + count_id + '-cnt').css({"display":"none","pointer-events":"none"});
            $('#' + count_id + '-klv').text('1');

            if(!$('div').is('.cart-block')) {
              $('.total-price').remove();
            }
          }

          //РѕР±С‰Р°СЏ СЃСѓРјРјР°
          var sum = 0;
          $('.cart-price').each(function(){
             sum += parseInt($(this).text());
          });
          $('.total-price p.price').text(sum + ' С‚Рі');
          $('span.total').text(sum + ' С‚Рі');
		  
		  //СЃР±РѕСЂ РґР°РЅРЅС‹С… РєРѕСЂР·РёРЅС‹ РІ РјР°СЃСЃРёРІ
		  var kuk_ar = [];
		  var kuk_key = 0;
		  $('.cart-block').each(function(){
			 var cb_id = $(this).attr('id')
			 var pizza = cb_id.substring(0, 7);
			 kuk_ar[kuk_key] = cb_id;
			 kuk_key++;
			 if (pizza=='crt-pzb') {
				 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'nm').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'prc').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + 'klv').text();
				 kuk_key++;
			 } else {
				 kuk_ar[kuk_key] = $('#' + cb_id).attr('data-cartart');
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-nm').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-prc').text();
				 kuk_key++;
				 kuk_ar[kuk_key] = $('#' + cb_id + '-klv').text();
				 kuk_key++;
			 }



			 
		  });
		 changecooksval("wowcart", kuk_ar);

          //РѕР±С‰РµРµ РєРѕР»РёС‡РµСЃС‚РІРѕ
          var kolvo = 0;
          $('.kolvo').each(function(){
              kolvo += parseInt($(this).text());
          });
          if (kolvo == 0) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
          if (kolvo == 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂ');
          if (kolvo > 1) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂР°');
		  if (kolvo > 4) $('span.quant').text(kolvo + ' С‚РѕРІР°СЂРѕРІ');
		  $('.cartCountProducts').text(kolvo);
        });

    //РѕС‚РєСЂС‹С‚СЊ РјРѕРґР°Р»РєСѓ --РЅР°С‡Р°Р»Рѕ--
      $('span.order').click(function(){
        $('div#shadow').css({"z-index":"999998", "opacity":"1"});
        $('form#form1').css({"z-index":"999999", "opacity":"1"});
      });

      //РѕС‚РєСЂС‹С‚СЊ РјРѕРґР°Р»РєСѓ --РєРѕРЅРµС†--

      //Р·Р°РєСЂС‹С‚СЊ РјРѕРґР°Р»РєСѓ --РЅР°С‡Р°Р»Рѕ--
      $('div#shadow').click(function(){
        $('div#shadow').css({"z-index":"-1", "opacity":"0"});
        $('form#form1').css({"z-index":"-1", "opacity":"0"});
      });

      $('img#closeform1').click(function(){
        $('div#shadow').css({"z-index":"-1", "opacity":"0"});
        $('form#form1').css({"z-index":"-1", "opacity":"0"});
      });
      //Р·Р°РєСЂС‹С‚СЊ РјРѕРґР°Р»РєСѓ --РєРѕРЅРµС†--
/*
      //РѕС‚РєСЂС‹С‚СЊ РјРѕРґР°Р»РєСѓ РєРѕСЂР·РёРЅС‹ --РЅР°С‡Р°Р»Рѕ--
	  $('a.showcart').click(function(){
        $('div#shadow').css({"z-index":"999998", "opacity":"1"});
        $('#checkoutedcart').css({"z-index":"999999", "opacity":"1"});

        var crt_block = $('.cart-block');
        // РїРѕР»СѓС‡Р°РµРј СЃРѕРґРµСЂР¶РёРјРѕРµ СЃРїРёСЃРєРѕРІ (РґР°РЅРЅС‹Рµ РІРЅСѓС‚СЂРё С‚РµРіРѕРІ)
        var vals = crt_block.map(function(){
          return $(this).attr('id');
        }).get();
        $('#cart-table').empty();
        $.each(vals,function(index,value){
          var row_id = value.toString();
          var row_name = $('#' + row_id).children(".name").text();
          var row_diam = $('#' + row_id).children(".diam").text();
          var row_quant = $('#' + row_id + 'klv').text();
          if (!row_quant >= 1) row_quant = $('#' + row_id + '-klv').text();
          var row_price = $('#' + row_id).children(".cart-price").text();
          $('#cart-table').append('<div class="rw-block"><h4 class="rw-name">' + row_name + '<span class="rw-diam">' + row_diam + '</span></h4><p class="rw-kolvo">' + row_quant + '</p><p  class="rw-price">' + row_price + '</p></div>');

          $("form#form2 textarea.hid").html('<div class="rw-block"><h4 class="rw-name">' + row_name + '<span class="rw-diam">' + row_diam + '</span></h4><p class="rw-kolvo">' + row_quant + '</p><p  class="rw-price">' + row_price + '</p></div>')
        });
        var itogo = $('#cart p.price').text();
        $('span#cartsumm').text(itogo);
      });

      //РѕС‚РєСЂС‹С‚СЊ РјРѕРґР°Р»РєСѓ РєРѕСЂР·РёРЅС‹ --РєРѕРЅРµС†--

      //Р·Р°РєСЂС‹С‚СЊ РјРѕРґР°Р»РєСѓ РєРѕСЂР·РёРЅС‹ --РЅР°С‡Р°Р»Рѕ--
      $('div#shadow').click(function(){
        $('div#shadow').css({"z-index":"-1", "opacity":"0"});
        $('#checkoutedcart').css({"z-index":"-1", "opacity":"0"});
      });

      $('span.goback').click(function(){
        $('div#shadow').css({"z-index":"-1", "opacity":"0"});
        $('#checkoutedcart').css({"z-index":"-1", "opacity":"0"});
      });

      $('img#closeform2').click(function(){
        $('div#shadow').css({"z-index":"-1", "opacity":"0"});
        $('#checkoutedcart').css({"z-index":"-1", "opacity":"0"});
      });
      //Р·Р°РєСЂС‹С‚СЊ РјРѕРґР°Р»РєСѓ РєРѕСЂР·РёРЅС‹ --РєРѕРЅРµС†--
	  */
	 
});

function setCookie (name, value, expires, path, domain, secure) {
		document.cookie = name + "=" + escape(value) +
		((expires) ? "; expires=" + expires : "") +
		((path) ? "; path=" + path : "") +
		((domain) ? "; domain=" + domain : "") +
		((secure) ? "; secure" : "");
	}

	function getCookie(name) {
		var cookie = " " + document.cookie;
		var search = " " + name + "=";
		var setStr = null;
		var offset = 0;
		var end = 0;
		if (cookie.length > 0) {
			offset = cookie.indexOf(search);
			if (offset != -1) {
				offset += search.length;
				end = cookie.indexOf(";", offset)
				if (end == -1) {
					end = cookie.length;
				}
				setStr = unescape(cookie.substring(offset, end));
			}
		}
		return(setStr);
	}

	function changecooksval(name, value){
		setCookie(name, value, "Mon, 01-Jan-2021 00:00:00 GMT", "/", window.location.hostname);
	}
